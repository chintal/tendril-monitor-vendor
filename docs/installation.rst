============
Installation
============

At the command line::

    $ easy_install tendril-monitor-vendor

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tendril
    $ pip install tendril-monitor-vendor
